import { EditableContentDirective } from './editable-content.directive';

describe('EditableContentDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableContentDirective();
    expect(directive).toBeTruthy();
  });
});
